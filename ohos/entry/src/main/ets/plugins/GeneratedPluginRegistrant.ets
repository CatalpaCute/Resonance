import { FlutterEngine } from '@ohos/flutter_ohos';

export class GeneratedPluginRegistrant {
  static registerWith(_flutterEngine: FlutterEngine): void {
    // The OpenHarmony Flutter tool rewrites this file from pubspec plugins.
    // Keep the placeholder so DevEco and early hvigor sync can resolve imports.
  }
}
